import PageStub from '../components/PageStub'

export default function Features() {
  return (
    <PageStub
      taskId="MKT-FR-2"
      eyebrow="Features"
      title="Purpose-built for every role in your operation."
      intro="One platform, role-based access. Everyone sees exactly what they need — and nothing they shouldn't."
    />
  )
}
