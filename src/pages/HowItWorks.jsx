import PageStub from '../components/PageStub'

export default function HowItWorks() {
  return (
    <PageStub
      taskId="MKT-FR-3"
      eyebrow="How it works"
      title="Six stages. One unbroken record."
      intro="FarmTally runs procurement as a deterministic workflow: every stage requires the right role's approval and the right evidence before the next stage unlocks."
    />
  )
}
